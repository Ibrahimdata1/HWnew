import { Button } from "@mui/material"
import { Link } from 'react-router-dom';

const ButtonNorm = () => {
  return (
   <Button color="error"><Link to={'/'}>Back HomePage</Link></Button>
  )
}

export default ButtonNorm
